var pos = {
    x: window.innerWidth - 370,
    y: window.innerHeight / 2 + 430
}

var goal = {
    x: window.innerWidth,
    y: window.innerHeight / 2
}

// addEventListener('keyup', () => {
//     if (event.keyCode == '32')
//         createBox(world, pos.x, pos.y, 15, 30, false);
// })
// addEventListener('mousedown', () => {
//     // createBox(world, event.clientX, event.clientY, 15, 30, false, goal.x, goal.y);
// })